<article class="recommended article container">
  <div class="row">
    <section class="col-md-4">
      <h2><%= article.title %></h2>
      <span class="meta">
        <%= article.published_at %>,
        <%= article.author %>
      </span>
    </section>
    <section class="content col-md-8">
      <p><%= article.paragraphs(1).first.html_safe %></p>

      <%= link_to 'Czytaj więcej', article_path(article), class: 'button button-secondary' %>
    </section>
  </div>
</article>
